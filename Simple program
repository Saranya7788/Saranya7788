#include <stdio.h>

int main() {
    int rows = 5;

    // Outer loop for rows
    for (int i = rows; i >= 1; i--) {
        // Inner loop for columns
        for (int j = 1; j <= i; j++) {
            printf("*");
        }
        printf("\n");
    }

    return 0;
}
